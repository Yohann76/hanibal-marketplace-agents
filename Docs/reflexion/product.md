# Product 

- Abonnement per agent ? (X/Month + limite usage?)
- Graph Agent (multi agent) or simple agent with tools ? 
- Where agents run ? In  hanibal website, chrome extension (for + context) (this option is limit..)
- Other developer can publish a other agent on marketplace ? (and touch fee..?)

# Design 

- Need logo 
- Need Website
- Need comunication with social network
- if other developer can create agent, need documentation 
